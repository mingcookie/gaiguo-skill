interface:
  display_name: "改过"
  short_description: "从错题到人生复盘，把错误变成位置、路径和下一步行动。"
  default_prompt: "Use $gaiguo to locate this error, identify its real cause, and define a correction with verification."
